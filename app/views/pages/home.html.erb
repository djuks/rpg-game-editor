<div class="jumbotron center" >
  <h1>Welcome to RPG editor</h1>
  <h2>Create your own heroes today!</h2>
  <p>
    <% if user_signed_in? %>
      <%= link_to "Sign up or Log in", root_path, class: "btn btn-danger btn-lg", id: "logo" %>
    <% else %>
      <%= link_to "Sign up or Log in",new_user_registration_path, class: "btn btn-danger btn-lg", id: "logo" %>
    <% end %>
  </p>
</div>
